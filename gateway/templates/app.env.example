# Export these values before launching the app when using the self-hosted gateway.
export FRD_ENABLE_PYDANTIC_AI_GATEWAY=true
export PYDANTIC_AI_GATEWAY_BASE_URL=https://gateway.example.com
export PYDANTIC_AI_GATEWAY_API_KEY=replace-with-gateway-app-key
